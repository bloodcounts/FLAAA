services:
  pdp:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PDP_PORT=8080
      - LOG_LEVEL=info
      - NODE_ENV=production
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
